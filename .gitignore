# Accidental / local write probes
**/.write-test

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Local env and secrets
.env
.env.*
*.local

# Build output (keep directory via .gitkeep)
builds/zips/*.zip

# Node (if added later)
node_modules/

# Composer vendor (if added later)
vendor/

# Tests / coverage (if added later)
phpunit.xml
.phpunit.result.cache
coverage/

# Logs and dumps
*.log
*.sql
*.sqlite
